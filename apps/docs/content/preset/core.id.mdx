---
title: Inti
description: Konfigurasi dasar Ultracite dengan aturan linting yang tidak tergantung pada framework.
---

Konfigurasi inti Ultracite menyediakan seperangkat aturan linting dan pengaturan pemformatan yang komprehensif dan tidak tergantung pada framework. Ini adalah fondasi yang diturunkan oleh semua konfigurasi spesifik framework lainnya.

## Instalasi

Konfigurasi inti sudah disertakan secara otomatis ketika Anda menjalankan:

```bash
npx ultracite init
```

Atau tambahkan secara manual ke `biome.jsonc` Anda:

```jsonc title="biome.jsonc"
{
  "extends": ["ultracite/core"]
}
```

## Ikhtisar

Konfigurasi inti mencakup ratusan aturan linting ketat yang meliputi:

- **Aksesibilitas (a11y)**: Menegakkan atribut ARIA, HTML semantik, dan navigasi keyboard
- **Kompleksitas**: Mengurangi kompleksitas kognitif dan menegakkan praktik terbaik
- **Kebenaran**: Mencegah kesalahan umum dan menegakkan keselamatan tipe
- **Performa**: Mengoptimalkan kode untuk performa run-time yang lebih baik
- **Keamanan**: Mencegah kerentanan keamanan
- **Gaya**: Menegakkan gaya kode yang konsisten
- **Mencurigakan**: Menangkap pola mencurigakan yang mungkin menunjukkan bug

## Pengaturan Formatter

- **Indentasi**: 2 spasi
- **Lebar Baris**: 80 karakter
- **Akhir Baris**: LF (gaya Unix)
- **Titik Koma**: Selalu diperlukan
- **Tanda Kutip**: Tanda kutip ganda untuk JavaScript/TypeScript dan JSON
- **Koma Akhir**: Gaya ES5 (array/objek multi-baris)
- **Kurung Panah**: Selalu sertakan tanda kurung

## Fitur Utama

### Ketatnya TypeScript

Konfigurasi inti mengaktifkan pemeriksaan ketat TypeScript:

- Mengurangi penggunaan `any` (`noExplicitAny: error`)
- Mengharuskan penanganan `null/undefined`
- Memprioritaskan tipe eksplisit dalam situasi tertentu
- Menegakkan keselamatan tipe di seluruh basis kode

### Padanan yang Disarankan ESLint

Aturan praktik terbaik umum disertakan:

- Tidak ada variabel yang tidak digunakan (`noUnusedVariables: error`)
- Tidak ada impor yang tidak digunakan (`noUnusedImports: error`)
- Tidak ada eval eksplisit (`noGlobalEval: error`)
- Tidak ada prototype pollution (`noPrototypeBuiltins: error`)

### Aksesibilitas Secara Default

Aturan yang setara dengan standar aksesibilitas:

- Atribut ARIA harus valid
- Elemen HTML semantik lebih disukai
- Dukungan navigasi keyboard diwajibkan
- Teks alt untuk gambar diwajibkan

### Dukungan Node.js

Penanganan spesifik lingkungan:

- Mengenali global Node.js (`module`, `process`)
- Menegakkan protokol `node:` untuk modul bawaan
- Mendukung CommonJS dan ESM

## Referensi Aturan Lengkap

### Aksesibilitas (a11y)

| Rule | Setting | Description |
|------|---------|-------------|
| `noAccessKey` | `error` | Memastikan atribut `accessKey` tidak digunakan pada elemen HTML manapun. |
| `noAriaHiddenOnFocusable` | `error` | Memastikan `aria-hidden="true"` tidak disetel pada elemen yang dapat difokuskan. |
| `noAriaUnsupportedElements` | `error` | Memastikan elemen yang tidak mendukung peran, status, dan properti ARIA tidak memiliki atribut tersebut. |
| `noAutofocus` | `off` | Memastikan properti `autoFocus` tidak digunakan pada elemen. |
| `noDistractingElements` | `error` | Menegakkan agar tidak digunakan elemen yang mengganggu. |
| `noHeaderScope` | `error` | Properti scope seharusnya hanya digunakan pada elemen `<th>`. |
| `noInteractiveElementToNoninteractiveRole` | `error` | Memastikan peran ARIA non-interaktif tidak diberikan ke elemen HTML interaktif. |
| `noLabelWithoutControl` | `error` | Memastikan elemen atau komponen label memiliki teks label dan input yang terkait. |
| `noNoninteractiveElementInteractions` | `error` | Melarang penggunaan event handler pada elemen non-interaktif. |
| `noNoninteractiveElementToInteractiveRole` | `error` | Memastikan peran ARIA interaktif tidak diberikan ke elemen HTML non-interaktif. |
| `noNoninteractiveTabindex` | `error` | Memastikan `tabIndex` tidak diberikan ke elemen HTML non-interaktif. |
| `noPositiveTabindex` | `error` | Mencegah penggunaan bilangan bulat positif pada properti `tabIndex`. |
| `noRedundantAlt` | `error` | Memastikan properti alt pada img tidak berisi kata `image`, `picture`, atau `photo`. |
| `noRedundantRoles` | `error` | Memastikan properti role eksplisit tidak sama dengan role implisit/default pada sebuah elemen. |
| `noStaticElementInteractions` | `error` | Memastikan elemen statis dan terlihat (seperti `<div>`) yang memiliki handler klik menggunakan atribut role yang valid. |
| `noSvgWithoutTitle` | `error` | Mewajibkan penggunaan elemen title untuk elemen `svg`. |
| `useAltText` | `error` | Memastikan semua elemen yang memerlukan teks alternatif memiliki informasi bermakna untuk disampaikan kepada pengguna akhir. |
| `useAnchorContent` | `error` | Memastikan anchor memiliki konten dan konten tersebut dapat diakses oleh pembaca layar. |
| `useAriaActivedescendantWithTabindex` | `error` | Memastikan `tabIndex` diberikan pada elemen HTML non-interaktif yang menggunakan aria-activedescendant. |
| `useAriaPropsForRole` | `error` | Memastikan elemen dengan peran ARIA harus memiliki semua atribut ARIA yang diperlukan untuk peran tersebut. |
| `useAriaPropsSupportedByRole` | `error` | Memastikan properti ARIA valid untuk peran yang didukung oleh elemen. |
| `useButtonType` | `error` | Mewajibkan penggunaan atribut `type` untuk elemen button. |
| `useFocusableInteractive` | `error` | Elemen dengan peran interaktif dan handler interaksi harus dapat difokuskan. |
| `useHeadingContent` | `error` | Memastikan elemen heading (`h1`, `h2`, dll.) memiliki konten dan konten tersebut dapat diakses oleh pembaca layar. |
| `useHtmlLang` | `error` | Memastikan elemen `html` memiliki atribut `lang`. |
| `useIframeTitle` | `error` | Mewajibkan penggunaan atribut title untuk elemen iframe. |
| `useKeyWithClickEvents` | `error` | Memastikan `onClick` disertai setidaknya salah satu dari: `onKeyUp`, `onKeyDown`, `onKeyPress`. |
| `useKeyWithMouseEvents` | `error` | Memastikan `onMouseOver` / `onMouseOut` disertai `onFocus` / `onBlur`. |
| `useMediaCaption` | `error` | Mewajibkan elemen audio dan video memiliki track untuk teks keterangan. |
| `useSemanticElements` | `error` | Mendeteksi penggunaan atribut role pada elemen JSX dan menyarankan penggunaan elemen semantik sebagai gantinya. |
| `useValidAnchor` | `error` | Memastikan semua anchor valid dan dapat dinavigasi. |
| `useValidAriaProps` | `error` | Memastikan properti ARIA `aria-*` semuanya valid. |
| `useValidAriaRole` | `error` | Elemen dengan peran ARIA harus menggunakan peran ARIA yang valid dan tidak abstrak. |
| `useValidAriaValues` | `error` | Memastikan nilai state dan properti ARIA valid. |
| `useValidAutocomplete` | `error` | Gunakan nilai yang valid untuk atribut autocomplete pada elemen input. |
| `useValidLang` | `error` | Memastikan nilai yang diberikan ke atribut `lang` adalah kode bahasa dan/atau negara ISO yang benar. |
| `useGenericFontNames` | `error` | Melarang tidak adanya kata kunci family generik dalam keluarga font (CSS). |

### Kompleksitas

| Rule | Setting | Description |
|------|---------|-------------|
| `noAdjacentSpacesInRegex` | `error` | Melarang penggunaan yang tidak jelas dari karakter spasi berturut-turut dalam literal ekspresi reguler. |
| `noArguments` | `error` | Melarang penggunaan arguments. |
| `noBannedTypes` | `error` | Melarang alias tipe primitif dan tipe yang menyesatkan. |
| `noCommaOperator` | `error` | Melarang operator koma. |
| `noEmptyTypeParameters` | `error` | Melarang parameter tipe kosong dalam alias tipe dan interface. |
| `noExcessiveCognitiveComplexity` | `error` | Melarang fungsi yang melebihi skor Cognitive Complexity tertentu. |
| `noExcessiveLinesPerFunction` | `off` | Membatasi jumlah baris kode dalam sebuah fungsi. |
| `noExcessiveNestedTestSuites` | `error` | Aturan ini menegakkan kedalaman maksimum untuk nested `describe()` dalam file test. |
| `noExtraBooleanCast` | `error` | Melarang cast boolean yang tidak perlu. |
| `noFlatMapIdentity` | `error` | Melarang penggunaan callback yang tidak perlu pada flatMap. |
| `noForEach` | `error` | Memprioritaskan penggunaan pernyataan `for...of` daripada `Array.forEach`. |
| `noImplicitCoercions` | `off` | Melarang konversi tipe shorthand. |
| `noImportantStyles` | `off` | Melarang penggunaan style `!important` (CSS). |
| `noStaticOnlyClass` | `error` | Aturan ini melaporkan ketika sebuah kelas tidak memiliki anggota non-statis. |
| `noThisInStatic` | `error` | Melarang `this` dan `super` dalam konteks statis. |
| `noUselessCatch` | `error` | Melarang klausa catch yang tidak perlu. |
| `noUselessConstructor` | `error` | Melarang konstruktor yang tidak perlu. |
| `noUselessContinue` | `error` | Menghindari penggunaan continue yang tidak perlu. |
| `noUselessEmptyExport` | `error` | Melarang ekspor kosong yang tidak mengubah apa pun di file modul. |
| `noUselessEscapeInRegex` | `error` | Melarang urutan escape yang tidak perlu dalam literal ekspresi reguler. |
| `noUselessFragments` | `error` | Melarang fragmen yang tidak perlu. |
| `noUselessLabel` | `error` | Melarang label yang tidak perlu. |
| `noUselessLoneBlockStatements` | `error` | Melarang pernyataan blok bersarang yang tidak perlu. |
| `noUselessRename` | `error` | Melarang mengubah nama impor, ekspor, dan assignment destrukturisasi menjadi nama yang sama. |
| `noUselessStringConcat` | `error` | Melarang penggabungan string atau template literal yang tidak perlu. |
| `noUselessStringRaw` | `error` | Melarang penggunaan `String.raw` yang tidak perlu dalam template string tanpa sequence escape. |
| `noUselessSwitchCase` | `error` | Melarang case yang tidak berguna dalam pernyataan switch. |
| `noUselessTernary` | `error` | Melarang operator ternary ketika alternatif yang lebih sederhana tersedia. |
| `noUselessThisAlias` | `error` | Melarang alias `this` yang tidak berguna. |
| `noUselessTypeConstraint` | `error` | Melarang penggunaan `any` atau `unknown` sebagai batas tipe. |
| `noUselessUndefinedInitialization` | `error` | Melarang inisialisasi variabel ke `undefined`. |
| `noVoid` | `error` | Melarang penggunaan operator `void`, yang bukan operator yang umum. |
| `useArrowFunction` | `error` | Gunakan arrow function daripada function expression. |
| `useDateNow` | `error` | Gunakan `Date.now()` untuk mendapatkan jumlah milidetik sejak Unix Epoch. |
| `useFlatMap` | `error` | Menganjurkan penggunaan `.flatMap()` ketika `map().flat()` digunakan bersama. |
| `useIndexOf` | `error` | Memprioritaskan `Array#{indexOf,lastIndexOf}()` daripada `Array#{findIndex,findLastIndex}()` ketika mencari indeks sebuah item. |
| `useLiteralKeys` | `error` | Menegakkan penggunaan akses literal ke properti daripada akses properti terkomputasi. |
| `useNumericLiterals` | `error` | Melarang `parseInt()` dan `Number.parseInt()` demi literal biner, oktal, dan heksadesimal. |
| `useOptionalChain` | `error` | Menegakkan penggunaan optional chaining yang ringkas daripada ekspresi logika berantai. |
| `useRegexLiterals` | `error` | Menegakkan penggunaan literal ekspresi reguler daripada konstruktor `RegExp` jika memungkinkan. |
| `useSimpleNumberKeys` | `error` | Melarang nama anggota objek literal angka yang bukan basis 10 atau menggunakan underscore sebagai pemisah. |
| `useSimplifiedLogicExpression` | `error` | Menghapus istilah redundan dari ekspresi logika. |
| `useWhile` | `error` | Menegakkan penggunaan loop while daripada for ketika inisialisator dan ekspresi pembaruan tidak diperlukan. |

### Kebenaran

| Rule | Setting | Description |
|------|---------|-------------|
| `noConstAssign` | `error` | Mencegah variabel const di-assign ulang. |
| `noConstantCondition` | `error` | Melarang ekspresi konstan dalam kondisi. |
| `noConstantMathMinMaxClamp` | `error` | Melarang penggunaan `Math.min` dan `Math.max` untuk meng-clamp nilai ketika hasilnya sendiri bersifat konstan. |
| `noConstructorReturn` | `error` | Melarang mengembalikan nilai dari konstruktor. |
| `noEmptyCharacterClassInRegex` | `error` | Melarang kelas karakter kosong dalam literal ekspresi reguler. |
| `noEmptyPattern` | `error` | Melarang pola destrukturisasi kosong. |
| `noGlobalDirnameFilename` | `error` | Melarang penggunaan `__dirname` dan `__filename` di scope global. |
| `noGlobalObjectCalls` | `error` | Melarang memanggil properti objek global sebagai fungsi. |
| `noInnerDeclarations` | `error` | Melarang deklarasi function dan `var` yang dapat diakses di luar bloknya. |
| `noInvalidBuiltinInstantiation` | `error` | Memastikan builtins diinstansiasi dengan benar. |
| `noInvalidConstructorSuper` | `error` | Mencegah penggunaan `super()` yang salah di dalam kelas. |
| `noInvalidDirectionInLinearGradient` | `error` | Melarang nilai arah non-standar untuk fungsi linear gradient (CSS). |
| `noInvalidGridAreas` | `error` | Melarang area grid bernama yang tidak valid dalam CSS Grid Layouts (CSS). |
| `noInvalidPositionAtImportRule` | `error` | Melarang penggunaan at-rule `@import` pada posisi yang tidak valid (CSS). |
| `noInvalidUseBeforeDeclaration` | `error` | Melarang penggunaan variabel dan parameter fungsi sebelum deklarasinya. |
| `noMissingVarFunction` | `error` | Melarang tidak adanya fungsi var untuk variabel css (CSS). |
| `noNodejsModules` | `off` | Melarang penggunaan modul bawaan Node.js. |
| `noNonoctalDecimalEscape` | `error` | Melarang urutan escape `\8` dan `\9` dalam literal string. |
| `noPrecisionLoss` | `error` | Melarang angka literal yang kehilangan presisi. |
| `noPrivateImports` | `off` | Membatasi impor dari ekspor privat. |
| `noProcessGlobal` | `off` | Melarang penggunaan global `process`. |
| `noRestrictedElements` | `error` | Melarang penggunaan elemen yang dikonfigurasi. |
| `noSelfAssign` | `error` | Melarang assignment di mana kedua sisi persis sama. |
| `noSetterReturn` | `error` | Melarang mengembalikan nilai dari setter. |
| `noStringCaseMismatch` | `error` | Melarang perbandingan ekspresi yang memodifikasi case string dengan nilai yang tidak sesuai. |
| `noSwitchDeclarations` | `error` | Melarang deklarasi leksikal dalam klausa switch. |
| `noUndeclaredDependencies` | `off` | Melarang penggunaan dependensi yang tidak tercantum di `package.json`. |
| `noUndeclaredVariables` | `error` | Mencegah penggunaan variabel yang belum dideklarasikan di dalam dokumen. |
| `noUnknownFunction` | `error` | Melarang fungsi nilai CSS yang tidak dikenal (CSS). |
| `noUnknownMediaFeatureName` | `error` | Melarang nama fitur media yang tidak dikenal (CSS). |
| `noUnknownProperty` | `error` | Melarang properti yang tidak dikenal (CSS). |
| `noUnknownPseudoClass` | `error` | Melarang selector pseudo-class yang tidak dikenal (CSS). |
| `noUnknownPseudoElement` | `error` | Melarang selector pseudo-element yang tidak dikenal (CSS). |
| `noUnknownTypeSelector` | `error` | Melarang selector tipe yang tidak dikenal (CSS). |
| `noUnknownUnit` | `error` | Melarang unit CSS yang tidak dikenal (CSS). |
| `noUnmatchableAnbSelector` | `error` | Melarang selector An+B yang tidak dapat dipasangkan (CSS). |
| `noUnreachable` | `error` | Melarang kode yang tidak dapat dijangkau. |
| `noUnreachableSuper` | `error` | Memastikan `super()` constructor dipanggil tepat sekali pada setiap jalur kode dalam konstruktor kelas sebelum properti ini diakses. |
| `noUnsafeFinally` | `error` | Melarang pernyataan kontrol alur di blok finally. |
| `noUnsafeOptionalChaining` | `error` | Melarang penggunaan optional chaining di konteks di mana nilai undefined tidak diperbolehkan. |
| `noUnusedFunctionParameters` | `error` | Melarang parameter fungsi yang tidak digunakan. |
| `noUnusedImports` | `error` (safe fix override) | Melarang impor yang tidak digunakan. |
| `noUnusedLabels` | `error` | Melarang label yang tidak digunakan. |
| `noUnusedPrivateClassMembers` | `error` | Melarang anggota private kelas yang tidak digunakan. |
| `noUnusedVariables` | `error` | Melarang variabel yang tidak digunakan. |
| `noVoidTypeReturn` | `error` | Melarang mengembalikan nilai dari fungsi dengan tipe return `void`. |
| `useGraphqlNamedOperations` | `error` | Mewajibkan penamaan operasi GraphQL. |
| `useImageSize` | `error` | Mewajibkan elemen `<img>` memiliki atribut width dan height. |
| `useImportExtensions` | `off` | Menegakkan ekstensi file untuk impor relatif. |
| `useIsNan` | `error` | Mewajibkan pemanggilan `isNaN()` saat memeriksa NaN. |
| `useJsonImportAttributes` | `off` | Mewajibkan penggunaan `{ type: "json" }` untuk impor modul JSON. |
| `useParseIntRadix` | `error` | Menegakkan penggunaan argumen radix yang konsisten saat menggunakan `parseInt()`. |
| `useSingleJsDocAsterisk` | `error` | Menegakkan baris komentar JSDoc dimulai dengan satu asterisk, kecuali baris pertama. |
| `useUniqueElementIds` | `off` | Mencegah penggunaan atribut id literal string statis pada elemen. |
| `useValidForDirection` | `error` | Menegakkan klausul pembaruan loop `for` memindahkan penghitung ke arah yang benar. |
| `useValidTypeof` | `error` | Aturan ini memeriksa bahwa hasil ekspresi `typeof` dibandingkan dengan nilai yang valid. |
| `useYield` | `error` | Mewajibkan fungsi generator mengandung `yield`. |

### Nursery

Aturan Nursery umumnya tidak diaktifkan di Ultracite karena bersifat eksperimental dan dapat berubah atau dihapus di versi Biome mendatang. Namun, aturan berikut diaktifkan karena permintaan populer:

| Rule | Setting | Description |
|------|---------|-------------|
| `useSortedClasses` | `error` (safe fix override) | Menegakkan pengurutan kelas utilitas CSS. |

### Performa

| Rule | Setting | Description |
|------|---------|-------------|
| `noAccumulatingSpread` | `error` | Melarang penggunaan sintaks spread (`...`) pada accumulator. |
| `noAwaitInLoops` | `off` | Melarang `await` di dalam loop. |
| `noBarrelFile` | `error` | Melarang penggunaan barrel file. |
| `noDelete` | `error` | Melarang penggunaan operator `delete`. |
| `noDynamicNamespaceImportAccess` | `error` | Melarang mengakses import namespace secara dinamis. |
| `noNamespaceImport` | `error` | Melarang penggunaan import namespace. |
| `noReExportAll` | `off` | Menghindari re-export all. |
| `noUnwantedPolyfillio` | `error` | Mencegah duplikasi polyfill dari Polyfill.io. |
| `useGoogleFontPreconnect` | `error` | Memastikan atribut `preconnect` digunakan saat memakai Google Fonts. |
| `useTopLevelRegex` | `error` | Mewajibkan literal regex dideklarasikan pada top level. |

### Keamanan

| Rule | Setting | Description |
|------|---------|-------------|
| `noBlankTarget` | `error` | Melarang atribut `target="_blank"` tanpa `rel="noopener"`. |
| `noDangerouslySetInnerHtml` | `error` | Mencegah penggunaan prop JSX yang berbahaya. |
| `noDangerouslySetInnerHtmlWithChildren` | `error` | Melaporkan ketika sebuah elemen DOM atau komponen menggunakan children dan prop `dangerouslySetInnerHTML` sekaligus. |
| `noGlobalEval` | `error` | Melarang penggunaan `eval()` global. |

### Gaya

| Rule | Setting | Description |
|------|---------|-------------|
| `noCommonJs` | `off` | Melarang penggunaan sistem modul Common JS demi impor gaya ESM. |
| `noDefaultExport` | `off` | Melarang default export. |
| `noDoneCallback` | `error` | Melarang penggunaan callback di test dan hook asinkron. |
| `noEnum` | `error` | Melarang enum TypeScript. |
| `noExportedImports` | `error` | Melarang mengekspor variabel yang diimpor. |
| `noImplicitBoolean` | `off` | Melarang nilai true implisit pada atribut boolean JSX. |
| `noInferrableTypes` | `error` | Melarang anotasi tipe untuk variabel, parameter, dan properti kelas yang diinisialisasi dengan ekspresi literal. |
| `noMagicNumbers` | `off` | Melaporkan penggunaan "magic numbers" — angka yang digunakan langsung alih-alih ditetapkan ke konstanta bernama. |
| `noNamespace` | `error` | Melarang penggunaan namespace TypeScript. |
| `noNegationElse` | `error` | Melarang negasi dalam kondisi if jika memiliki klausa else. |
| `noNestedTernary` | `error` | Melarang ekspresi ternary bersarang. |
| `noNonNullAssertion` | `error` | Melarang non-null assertion menggunakan operator postfix `!`. |
| `noParameterAssign` | `error` | Melarang meng-assign ulang parameter fungsi. |
| `noParameterProperties` | `error` | Melarang penggunaan parameter properties dalam konstruktor kelas. |
| `noProcessEnv` | `off` | Melarang penggunaan `process.env`. |
| `noRestrictedGlobals` | `error` | Aturan ini memungkinkan Anda menentukan nama variabel global yang tidak ingin digunakan di aplikasi Anda. |
| `noRestrictedImports` | `error` | Melarang modul tertentu saat dimuat melalui import atau require. |
| `noRestrictedTypes` | `error` | Melarang tipe yang didefinisikan pengguna. |
| `noShoutyConstants` | `error` | Melarang penggunaan konstanta yang nilainya merupakan versi uppercase dari namanya. |
| `noSubstr` | `error` | Menegakkan penggunaan `String.slice()` daripada `String.substr()` dan `String.substring()`. |
| `noUnusedTemplateLiteral` | `error` | Melarang template literal jika interpolasi dan penanganan karakter khusus tidak diperlukan. |
| `noUselessElse` | `error` | Melarang blok else ketika blok if keluar lebih awal. |
| `noYodaExpression` | `error` | Melarang penggunaan yoda expressions. |
| `useArrayLiterals` | `error` | Melarang konstruktor `Array`. |
| `useAsConstAssertion` | `error` | Menegakkan penggunaan as const daripada tipe literal dan anotasi tipe. |
| `useAtIndex` | `error` | Gunakan `at()` sebagai pengganti akses indeks integer. |
| `useBlockStatements` | `error` | Memerlukan konvensi kurung kurawal yang konsisten. |
| `useCollapsedElseIf` | `error` | Menegakkan penggunaan `else if` daripada `if` bersarang di dalam klausa `else`. |
| `useCollapsedIf` | `error` | Menegakkan penggunaan satu `if` daripada klausa `if` bersarang. |
| `useComponentExportOnlyModules` | `off` | Menegakkan deklarasi komponen hanya dalam modul yang mengeksport React Components secara eksklusif. |
| `useConsistentArrayType` | `error` | Memerlukan konsistensi penggunaan `T[]` atau `Array<T>`. |
| `useConsistentBuiltinInstantiation` | `error` | Menegakkan penggunaan new untuk semua builtins, kecuali `String`, `Number` dan `Boolean`. |
| `useConsistentCurlyBraces` | `off` | Aturan ini menegakkan penggunaan kurung kurawal yang konsisten di dalam atribut JSX dan children JSX. |
| `useConsistentMemberAccessibility` | `error` | Memerlukan modifier aksesibilitas yang konsisten pada properti dan method kelas. |
| `useConsistentObjectDefinitions` | `error` | Memerlukan deklarasi literal objek yang konsisten. |
| `useConst` | `error` | Mewajibkan deklarasi const untuk variabel yang hanya diassign sekali. |
| `useDefaultParameterLast` | `error` | Menegakkan agar parameter default dan parameter opsional berada di akhir. |
| `useDefaultSwitchClause` | `error` | Mewajibkan klausa `default` dalam pernyataan switch. |
| `useDeprecatedReason` | `error` | Mewajibkan menyebutkan alasan ketika menggunakan direktif `@deprecated`. |
| `useEnumInitializers` | `error` | Mewajibkan setiap anggota enum diinisialisasi secara eksplisit. |
| `useExplicitLengthCheck` | `off` | Menegakkan perbandingan eksplisit terhadap properti `length`, `size`, `byteLength` atau `byteOffset` dari sebuah nilai. |
| `useExponentiationOperator` | `error` | Melarang penggunaan `Math.pow` demi operator `**`. |
| `useExportType` | `error` | Menganjurkan penggunaan export type untuk tipe. |
| `useExportsLast` | `off` | Mewajibkan semua ekspor dideklarasikan setelah semua pernyataan non-ekspor. |
| `useFilenamingConvention` | `error` | Menegakkan konvensi penamaan untuk file JavaScript dan TypeScript (kebab-case, ASCII). |
| `useForOf` | `error` | Aturan ini merekomendasikan loop for-of ketika pada for loop, indeks digunakan untuk mengambil item dari array yang diiterasi. |
| `useFragmentSyntax` | `error` | Aturan ini menegakkan penggunaan `<>...</>` daripada `<Fragment>...</Fragment>`. |
| `useGraphqlNamingConvention` | `error` | Memvalidasi bahwa semua nilai enum dikapitalisasi. |
| `useGroupedAccessorPairs` | `error` | Menegakkan getter dan setter untuk properti yang sama berdekatan dalam definisi kelas dan objek. |
| `useImportType` | `error` | Menganjurkan penggunaan `import type` untuk tipe. |
| `useLiteralEnumMembers` | `error` | Mewajibkan semua anggota enum berupa nilai literal. |
| `useNamingConvention` | `off` | Aturan konvensi penamaan (dinonaktifkan). |
| `useNodeAssertStrict` | `error` | Menganjurkan penggunaan `node:assert/strict` daripada `node:assert`. |
| `useNodejsImportProtocol` | `error` | Menegakkan penggunaan protokol `node:` untuk modul bawaan Node.js. |
| `useNumberNamespace` | `error` | Gunakan properti `Number` daripada yang global. |
| `useNumericSeparators` | `error` | Menegakkan penggunaan pemisah numerik pada literal angka. |
| `useObjectSpread` | `error` | Lebih memilih object spread daripada `Object.assign()` saat membuat objek baru. |
| `useReadonlyClassProperties` | `error` | Menegakkan menandai anggota sebagai readonly jika tidak dimodifikasi di luar konstruktor. |
| `useSelfClosingElements` | `error` | Mencegah tag penutup tambahan untuk komponen tanpa children. |
| `useShorthandAssign` | `error` | Mewajibkan shorthand operator assignment bila memungkinkan. |
| `useShorthandFunctionType` | `error` | Menegakkan penggunaan function types daripada type object dengan call signatures. |
| `useSingleVarDeclarator` | `off` | Melarang deklarasi beberapa variabel dalam satu pernyataan variabel. |
| `useSymbolDescription` | `error` | Mewajibkan parameter deskripsi untuk `Symbol()`. |
| `useTemplate` | `error` | Memprioritaskan template literals daripada penggabungan string. |
| `useThrowNewError` | `error` | Mewajibkan penggunaan new saat melempar error. |
| `useThrowOnlyError` | `error` | Melarang melempar nilai non-`Error`. |
| `useTrimStartEnd` | `error` | Menegakkan penggunaan `String.trimStart()` dan `String.trimEnd()` daripada `String.trimLeft()` dan `String.trimRight()`. |
| `useUnifiedTypeSignatures` | `error` | Melarang overload signatures yang dapat disatukan menjadi satu signature. |
| `noDescendingSpecificity` | `error` | Melarang selector dengan spesifisitas lebih rendah muncul setelah selector dengan spesifisitas lebih tinggi (CSS). |
| `noValueAtRule` | `error` | Melarang penggunaan at-rule `@value` dalam css modules (CSS). |

### Mencurigakan

| Rule | Setting | Description |
|------|---------|-------------|
| `noAlert` | `error` | Melarang penggunaan `alert`, `confirm`, dan `prompt`. |
| `noApproximativeNumericConstant` | `error` | Gunakan konstanta standar daripada literal yang diperkirakan. |
| `noAssignInExpressions` | `error` | Melarang assignment dalam ekspresi. |
| `noAsyncPromiseExecutor` | `error` | Melarang menggunakan fungsi async sebagai Promise executor. |
| `noBiomeFirstException` | `error` | Mencegah penggunaan pola `!` pada posisi pertama `files.includes` dalam file konfigurasi. |
| `noBitwiseOperators` | `error` | Melarang operator bitwise. |
| `noCatchAssign` | `error` | Melarang meng-assign ulang exception di dalam catch. |
| `noClassAssign` | `error` | Melarang meng-assign ulang anggota kelas. |
| `noCommentText` | `error` | Mencegah komentar dimasukkan sebagai node teks. |
| `noCompareNegZero` | `error` | Melarang membandingkan dengan `-0`. |
| `noConfusingLabels` | `error` | Melarang pernyataan berlabel yang bukan loop. |
| `noConfusingVoidType` | `error` | Melarang void type di luar generic atau return types. |
| `noConsole` | `off` | Melarang penggunaan `console`. |
| `noConstEnum` | `error` | Melarang const enum TypeScript. |
| `noConstantBinaryExpressions` | `error` | Melarang ekspresi di mana operasi tidak memengaruhi nilai. |
| `noControlCharactersInRegex` | `error` | Mencegah karakter kontrol dan beberapa sequence escape yang mencocokkan karakter kontrol dalam literal ekspresi reguler. |
| `noDebugger` | `error` | Melarang penggunaan debugger. |
| `noDocumentCookie` | `error` | Melarang penugasan langsung ke document.cookie. |
| `noDoubleEquals` | `error` | Mewajibkan penggunaan `===` dan `!==`. |
| `noDuplicateAtImportRules` | `error` | Melarang duplikasi aturan `@import` (CSS). |
| `noDuplicateCase` | `error` | Melarang label case duplikat. |
| `noDuplicateClassMembers` | `error` | Melarang anggota kelas duplikat. |
| `noDuplicateCustomProperties` | `error` | Melarang properti kustom duplikat dalam declaration blocks (CSS). |
| `noDuplicateElseIf` | `error` | Melarang kondisi duplikat dalam rantai if-else-if. |
| `noDuplicateFields` | `error` | Melarang field duplikat dalam operasi GraphQL. |
| `noDuplicateFontNames` | `error` | Melarang nama duplikat dalam keluarga font (CSS). |
| `noDuplicateObjectKeys` | `error` | Melarang dua kunci dengan nama yang sama di dalam objek. |
| `noDuplicateParameters` | `error` | Melarang nama parameter fungsi duplikat. |
| `noDuplicateProperties` | `error` | Melarang properti duplikat dalam declaration blocks (CSS). |
| `noDuplicateSelectorsKeyframeBlock` | `error` | Melarang selector duplikat dalam keyframe blocks (CSS). |
| `noDuplicateTestHooks` | `error` | Sebuah describe block tidak boleh berisi hook duplikat. |
| `noEmptyBlock` | `error` | Melarang block CSS kosong (CSS). |
| `noEmptyBlockStatements` | `error` | Melarang block statements dan static blocks yang kosong. |
| `noEmptyInterface` | `error` | Melarang deklarasi interface kosong. |
| `noEvolvingTypes` | `error` | Melarang variabel berubah menjadi tipe any melalui reassignments. |
| `noExplicitAny` | `error` | Melarang penggunaan tipe any. |
| `noExportsInTest` | `error` | Melarang penggunaan export atau module.exports dalam file yang berisi test. |
| `noExtraNonNullAssertion` | `error` | Mencegah penggunaan operator non-null assertion `!` yang salah di file TypeScript. |
| `noFallthroughSwitchClause` | `error` | Melarang fallthrough pada klausa switch. |
| `noFocusedTests` | `error` | Melarang test yang difokuskan. |
| `noFunctionAssign` | `error` | Melarang meng-assign ulang deklarasi function. |
| `noGlobalAssign` | `error` | Melarang assignment ke objek native dan variabel global yang read-only. |
| `noGlobalIsFinite` | `error` | Gunakan `Number.isFinite` daripada global `isFinite`. |
| `noGlobalIsNan` | `error` | Gunakan `Number.isNaN` daripada global `isNaN`. |
| `noImplicitAnyLet` | `error` | Melarang penggunaan implicit any pada deklarasi variabel. |
| `noImportAssign` | `error` | Melarang meng-assign ke binding yang diimpor. |
| `noImportantInKeyframe` | `error` | Melarang `!important` yang tidak valid dalam deklarasi keyframe (CSS). |
| `noIrregularWhitespace` | `error` | Melarang penggunaan karakter whitespace yang tidak beraturan. |
| `noLabelVar` | `error` | Melarang label yang memiliki nama yang sama dengan variabel. |
| `noMisleadingCharacterClass` | `error` | Melarang karakter yang dibuat dengan beberapa code point dalam sintaks kelas karakter. |
| `noMisleadingInstantiator` | `error` | Menegakkan penggunaan `new` dan `constructor` yang benar. |
| `noMisplacedAssertion` | `error` | Memeriksa bahwa fungsi assertion, misalnya expect, ditempatkan di dalam pemanggilan `it()`. |
| `noMisrefactoredShorthandAssign` | `error` | Melarang shorthand assign ketika variabel muncul di kedua sisi. |
| `noOctalEscape` | `error` | Melarang urutan escape oktal dalam literal string. |
| `noPrototypeBuiltins` | `error` | Melarang penggunaan langsung builtins dari `Object.prototype`. |
| `noQuickfixBiome` | `error` | Melarang penggunaan `quickfix.biome` di dalam file pengaturan editor. |
| `noRedeclare` | `error` | Melarang redeclare variabel, function, class, dan tipe dalam scope yang sama. |
| `noRedundantUseStrict` | `error` | Mencegah adanya `use strict` yang redundan. |
| `noSelfCompare` | `error` | Melarang perbandingan di mana kedua sisi persis sama. |
| `noShadowRestrictedNames` | `error` | Melarang identifier yang menimpa nama yang dibatasi. |
| `noShorthandPropertyOverrides` | `error` | Melarang shorthand properties yang menimpa longhand terkait (CSS). |
| `noSkippedTests` | `error` | Melarang test yang dinonaktifkan. |
| `noSparseArray` | `error` | Mencegah penggunaan sparse arrays (array dengan lubang). |
| `noTemplateCurlyInString` | `error` | Melarang sintaks placeholder template literal dalam string biasa. |
| `noThenProperty` | `error` | Melarang properti then. |
| `noTsIgnore` | `error` | Mencegah penggunaan directive TypeScript `@ts-ignore`. |
| `noUnassignedVariables` | `error` | Melarang variabel let atau var yang dibaca tetapi tidak pernah diassign. |
| `noUnknownAtRules` | `error` | Melarang at-rule yang tidak dikenal (CSS). |
| `noUnsafeDeclarationMerging` | `error` | Melarang penggabungan deklarasi yang tidak aman antara interface dan class. |
| `noUnsafeNegation` | `error` | Melarang penggunaan negasi yang tidak aman (`!`). |
| `noUselessEscapeInString` | `error` | Melarang escape yang tidak perlu dalam literal string. |
| `noUselessRegexBackrefs` | `error` | Melarang backreference regex yang selalu mencocokkan string kosong. |
| `noVar` | `error` | Melarang penggunaan `var`. |
| `noWith` | `error` | Melarang pernyataan `with` dalam konteks non-strict. |
| `useAdjacentOverloadSignatures` | `error` | Melarang penggunaan overload signatures yang tidak berdekatan. |
| `useAwait` | `error` | Memastikan fungsi async menggunakan `await`. |
| `useBiomeIgnoreFolder` | `error` | Menganjurkan penggunaan yang benar untuk mengabaikan folder dalam file konfigurasi. |
| `useDefaultSwitchClauseLast` | `error` | Menegakkan agar klausa default dalam switch berada di posisi terakhir. |
| `useErrorMessage` | `error` | Menegakkan penyertaan pesan saat membuat error bawaan. |
| `useGetterReturn` | `error` | Menegakkan agar method get selalu mengembalikan nilai. |
| `useGoogleFontDisplay` | `error` | Mewajibkan penggunaan strategi display yang direkomendasikan dengan Google Fonts. |
| `useGuardForIn` | `error` | Mewajibkan loop for-in menyertakan pernyataan `if`. |
| `useIsArray` | `error` | Gunakan `Array.isArray()` daripada `instanceof Array`. |
| `useIterableCallbackReturn` | `error` | Menegakkan nilai return yang konsisten dalam callback iterable. |
| `useNamespaceKeyword` | `error` | Mewajibkan penggunaan keyword namespace daripada keyword `module` untuk mendeklarasikan namespace TypeScript. |
| `useNumberToFixedDigitsArgument` | `error` | Menegakkan penggunaan argumen digits dengan `Number#toFixed()`. |
| `useStaticResponseMethods` | `error` | Gunakan method statis `Response` daripada konstruktor `new Response()` jika memungkinkan. |
| `useStrictMode` | `error` | Menegakkan penggunaan directive `use strict` dalam file skrip. |

## Catatan

Beberapa aturan dinonaktifkan karena terlalu lambat dijalankan dengan Scanner. Seperti yang dicatat dalam [dokumentasi Biome](https://biomejs.dev/guides/investigate-slowness/#first-things-first), aturan proyek yang diaktifkan diketahui menyebabkan overhead performa sebagai imbalan untuk analisis yang lebih mendalam.

Dalam pengujian kami, kami menemukan aturan-aturan ini dapat membuat perbedaan antara proses lint 300ms dan 13s.
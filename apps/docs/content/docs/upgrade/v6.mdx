---
title: Upgrade to v6
description: Guide for upgrading from Ultracite v5 to v6 with the new preset system.
---

Ultracite v6 introduces framework-specific presets that give you more control over which rules apply to your project.

## What Changed

v6 introduces [framework-specific presets](/configuration#framework-specific-configurations):

- [`ultracite/biome/core`](/preset/core) - Base JavaScript/TypeScript rules
- [`ultracite/biome/react`](/preset/react) - React-specific rules
- [`ultracite/biome/next`](/preset/next) - Next.js-specific rules
- [`ultracite/biome/solid`](/preset/solid) - Solid.js rules
- [`ultracite/biome/vue`](/preset/vue) - Vue.js rules
- [`ultracite/biome/svelte`](/preset/svelte) - Svelte rules
- [`ultracite/biome/qwik`](/preset/qwik) - Qwik rules
- [`ultracite/biome/angular`](/preset/angular) - Angular HTML parser
- [`ultracite/biome/remix`](/preset/remix) - File-based routing support

## Migration

### Next.js/React Projects

Update your `biome.jsonc` to extend the appropriate presets:

```jsonc title="biome.jsonc"
{
  "extends": ["ultracite/biome/core", "ultracite/biome/react", "ultracite/biome/next"]
}
```

### Non-React Projects

Switch to the core preset if you're not using React:

```jsonc title="biome.jsonc"
{
  "extends": ["ultracite/biome/core"]
}
```

### Other Frameworks

Use the appropriate preset for your framework:

```jsonc title="biome.jsonc"
{
  // Solid.js
  "extends": ["ultracite/biome/core", "ultracite/biome/solid"]

  // Vue.js
  "extends": ["ultracite/biome/core", "ultracite/biome/vue"]

  // Svelte
  "extends": ["ultracite/biome/core", "ultracite/biome/svelte"]

  // Qwik
  "extends": ["ultracite/biome/core", "ultracite/biome/qwik"]

  // Angular
  "extends": ["ultracite/biome/core", "ultracite/biome/angular"]
}
```

### Multi-Framework Projects

Combine presets as needed:

```jsonc title="biome.jsonc"
{
  "extends": ["ultracite/biome/core", "ultracite/biome/react", "ultracite/biome/solid"]
}
```

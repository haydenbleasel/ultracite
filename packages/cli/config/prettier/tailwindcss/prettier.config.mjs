/** @type {import('prettier-plugin-tailwindcss').PluginOptions} */
const config = {
  // Must be last in the plugins array when combined with other plugins
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
